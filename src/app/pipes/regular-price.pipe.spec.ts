import { RegularPricePipe } from './regular-price.pipe';

describe('RegularPricePipe', () => {
  it('create an instance', () => {
    const pipe = new RegularPricePipe();
    expect(pipe).toBeTruthy();
  });
});
